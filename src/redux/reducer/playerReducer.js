const INITIAL_STATE = {};

const playerReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  case 'ADD_EMAIL':
    break;
  default:
    return state;
  }
};

export default playerReducer;
